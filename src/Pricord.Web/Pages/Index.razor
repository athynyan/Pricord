@page "/"

<PageTitle>Home</PageTitle>

<AuthorizeView>
    <Authorized>
        <h1>Hello, @context.User.Claims.FirstOrDefault(c => c.Type == "name")?.Value!</h1>
    </Authorized>
    <NotAuthorized>
        <h1>Hello, World!</h1>
    </NotAuthorized>
</AuthorizeView>